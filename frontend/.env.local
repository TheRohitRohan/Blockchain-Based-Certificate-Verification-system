# ── Local development overrides ───────────────────────────────
# When set, Axios will use this URL directly (bypassing the Vite proxy).
# Use this to test against the cloud backend from your local machine.
# Leave commented out to use the Vite proxy → dummy backend.

# VITE_API_URL=https://your-cloud-backend.com/api
